# External Acme tls module

This module intends to give non proxmox cloud managed kubernetes clusters access to the certificate generation of an existing proxmox cloud instance.

After creating it requires to manually run the awx cert refreshing job on the target cloud.